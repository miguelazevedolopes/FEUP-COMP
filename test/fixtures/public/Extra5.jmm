import io;

class Test5 {
    public static void main(String[] args) {
        int t;
        int b;

        t = 0;
        b = 1;

        while (t < 5) {
            io.println(t);
            t = t + 1;
        }

        while (false) {
            io.println(54321);
        }

        while (b < 3 && t < 9) {
            io.println(t);
            t = t + 1;
            b = b + 1;
        }

        io.println(342);

        while (t < 5 + 5 + 3 - 1) {
            io.println(t);
            t = t + 1;
        }
    }
}